'use client';

import { Vector3 } from 'three';

export function Hallway({ position: _position }: { position: Vector3 | [number, number, number] }) {
  // Hallway is just an open space, no specific objects needed
  return null;
}
